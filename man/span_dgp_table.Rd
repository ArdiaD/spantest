% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgp_names.R
\name{span_dgp_table}
\alias{span_dgp_table}
\title{The simulation processes, by name and preset number}
\usage{
span_dgp_table()
}
\value{
A data frame with one row per process and columns \code{preset} (integer),
\code{name} (character), \code{innovation}, \code{dynamics}, \code{df} and \code{standardize}. The
last two record the preset's own settings: the Student-t presets \code{iid-ST}
and \code{AR-ST} use \emph{raw}, non-standardised \eqn{t_5} innovations, so they are
not reproduced by passing \code{innovation = "t"} with the default arguments.
}
\description{
Returns the correspondence between the names accepted by \code{\link[=span_simulate]{span_simulate()}}'s
\code{dgp} argument, the preset numbers, and the process each one denotes. Useful
when reading code or output that refers to a process by number.
}
\examples{
span_dgp_table()

# the same process, two ways
set.seed(1); a <- span_simulate(100, 2, 3, dgp = "AR-GARCH-SKST")
set.seed(1); b <- span_simulate(100, 2, 3, dgp = 9)
identical(a, b)

}
\seealso{
\code{\link[=span_simulate]{span_simulate()}}, \link{DGP_NAMES}
}
